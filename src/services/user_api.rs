// User API (MTProto) — stub for now
// Will be implemented with grammers when API is fully explored
// Currently using tg-proxy (Node.js + gramjs) as bridge

pub struct UserApi;

impl UserApi {
    pub fn is_available() -> bool {
        false
    }
}
