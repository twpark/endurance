[package]
name = "endurance"
version = "0.5.0"
edition = "2021"
authors = ["Teddy Park <taiwoo.park@gmail.com>", "cokac <monogatree@gmail.com>"]
description = "Agentic bot platform — Telegram bots with Claude/Gemini, structured message storage, and HTTP API"
license = "MIT"
repository = "https://github.com/twpark/endurance"
keywords = ["terminal", "cli", "file-manager", "multi-panel", "tui", "ai"]
categories = ["command-line-utilities", "filesystem"]

[dependencies]
ratatui = { version = "0.28", features = ["unstable-rendered-line-info"] }
crossterm = "0.28"
tokio = { version = "1", features = ["rt-multi-thread", "sync", "time", "process", "io-util", "macros", "net"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
chrono = { version = "0.4", default-features = false, features = ["std", "clock"] }
dirs = "5"
unicode-width = "0.2"
textwrap = "0.16"
image = "0.25"
regex = "1"
rand = "0.8"
supports-color = "3"
base64 = "0.22"
aes = "0.8"
cbc = { version = "0.1", features = ["alloc"] }
cipher = { version = "0.4", features = ["block-padding"] }
pbkdf2 = "0.12"
sha2 = "0.10"
hex = "0.4"
hmac = "0.12"
md-5 = "0.10"
thiserror = "1"
russh = "0.46"
russh-sftp = "2.0"
russh-keys = "0.46"
async-trait = "0.1"
ratatui-image = { version = "2.0", default-features = false, features = ["rustix", "image-defaults", "crossterm"] }
teloxide = { version = "0.13", default-features = false, features = ["macros", "rustls", "ctrlc_handler"] }
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls"] }
serenity = { version = "0.12", default-features = false, features = ["client", "gateway", "http", "model", "builder", "cache", "rustls_backend"] }
fs2 = "0.4"
filetime = "0.2"
rusqlite = { version = "0.31", features = ["bundled"] }

# Endurance additions
axum = "0.7"
tower-http = { version = "0.6", features = ["cors"] }
grammers-client = "0.9"
grammers-session = "0.9"
grammers-tl-types = "0.9"

[target.'cfg(unix)'.dependencies]
libc = "0.2"

[dev-dependencies]
tempfile = "3"

[[bin]]
name = "cokacdir"
path = "src/main.rs"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
strip = true

[lints.rust]
unsafe_code = "warn"

[lints.clippy]
unwrap_used = "warn"
expect_used = "warn"
